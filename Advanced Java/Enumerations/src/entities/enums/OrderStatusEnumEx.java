package entities.enums;

/* Enum is a special type that specifies a set of related constants.
* Enums make the code more readable, semantic and assisted by the compiler */

public enum OrderStatusEnumEx {
    // Each one of these is a constant of the enumeration "OrderStatusEnumEx"
    PENDING_PAYMENT,
    PROCESSING,
    SHIPPED,
    DELIVERED;
}
